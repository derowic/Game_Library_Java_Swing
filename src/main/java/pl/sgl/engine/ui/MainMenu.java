package pl.sgl.engine.ui;

import pl.sgl.engine.Scene;

/*public class MainMenu extends Scene {
    private UIPanel mainPanel;
    private UIPanel settingsPanel;

    @Override
    public void init() {
        mainPanel = new UIPanel(0, 0, 1280, 720);
        settingsPanel = new UIPanel(0, 0, 1280, 720);
        settingsPanel.visible = false;

        UIButton toSettings = new UIButton("USTAWIENIA", 500, 300, 200, 50);
        toSettings.setAction(() -> {
            mainPanel.visible = false;
            settingsPanel.visible = true;
        });

        mainPanel.addElement(toSettings);
        ui.addElement(mainPanel);
        ui.addElement(settingsPanel);
    }
    // ... reszta metod
}*/
